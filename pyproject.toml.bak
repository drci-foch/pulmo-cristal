[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "donneur_extractor"
version = "0.1.0"
description = "Extracteur de données des dossiers donneurs d'organes"
requires-python = ">=3.8"
dependencies = [
    "PyPDF2>=3.0.0",
    "camelot-py[cv]>=0.10.1", 
    "numpy",
    "pandas",
]